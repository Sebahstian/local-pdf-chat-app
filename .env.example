# Copy to .env and adjust if needed. All values shown are the defaults.

# Ollama server
OLLAMA_BASE_URL=http://localhost:11434

# Models (must be pulled in Ollama: `make pull-models`)
LLM_MODEL=llama3.2:3b
EMBED_MODEL=nomic-embed-text

# Retrieval tuning
CHUNK_SIZE=1000
CHUNK_OVERLAP=150
TOP_K=4
